class Solution:
    def reorderedPowerOf2(self, n: int) -> bool:
        target = sorted(str(n))
        for k in range(31):
            if sorted(str(1 << k)) == target :
                return True
        return False
